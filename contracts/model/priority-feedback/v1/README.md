# 单项反馈模型输出契约 v1

该契约只允许模型指出一个优先问题，并引用用户 V1 中真实存在的原文证据。

关键限制：

- `additionalProperties: false`，拒绝未约定字段；
- `replacement_text` 必须为 `null`，防止模型代写；
- `evidence_quote` 必须由网关再次验证为 V1 的连续子串；
- 合法 JSON 不代表可以直接交付，仍需通过预算、超时与证据校验；
- 持久化审计只保存证据偏移和哈希，不复制用户原文。
