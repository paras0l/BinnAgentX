# 连续训练与学习首页实现记录

> 状态：桌面 Web 可连续体验增量 v0.1
>
> 日期：2026-07-16
>
> 证据边界：证明首次体验完成后可以进入新材料的下一次读写闭环，并保留可理解的本机体验记录；不证明长期留存、选材教学有效性或稳定能力提升。

## 1. 本次完成的体验闭环

原有纵向切片在完成后只能返回首次背景，下一任务也只是不可执行的占位事实。本增量把主路径扩展为：

`首次背景 → 校准 A/B → 校准摘要 → 匹配阅读 → 微型表达 → 难度反馈 → 完成页 → 学习首页 → 新材料 practice 运行 → 匹配阅读 → 微型表达 → 收尾`

首次运行继续保留两段校准。后续运行不再重复 60 秒背景和两段校准，而是直接从匹配阅读开始；阅读工作区、语义标记、V1/H1/V2、表达单项反馈、草稿恢复、暂停和幂等命令仍沿用已有实现。

### 难度评价、当前适配水平与下一篇材料

阅读实验室沿用收尾页原有的“偏简单 / 刚刚好 / 偏困难”入口，不另建评价流程。评价和运行状态在同一事务中保存，并排入 `learner_level_assessments`；常驻 Worker 随后调用水平评测 Agent，汇总该账号已有的阅读作答正确性与题目难度、语法验证、任务完成、提示依赖、修订、标注以及历次主观难度反馈。

文章正文末尾另有一次性的“有帮助 / 没帮助”即时反馈，作用类似 Good response / Bad response：用户仍在阅读区域内即可提交，不必等到整次训练收尾。`material_feedback_events.task_id` 唯一约束保证每篇只能记录一次，重复或并发请求返回第一次选择，不允许事后覆盖。即时反馈会以独立 `trigger_key` 排入同一个水平评测队列；因此同一运行可以在文章反馈后记录一次画像证据，并在之后提交难度评价时结合新增学习行为再次重评。即时帮助度与收尾难度是两种不同证据，前者不替代后者。

“没帮助”可能意味着题材不相关、语境不自然或生成质量不佳，不能推断学习者能力较低。因此水平评测 Agent 会保留即时反馈原因码，但不会用 Good/Bad 升降能力档位或增加能力证据数；下一次个性化材料生成会单独读取最近即时反馈，用于改善目标相关性、语境自然度和可理解性。客观作答与语法验证是能力估计的主要证据，明确的难度评价只做有界负荷校正，其他学习行为只用于过程描述。

评测结果称为“当前适配水平”，不是考试分数或正式 CEFR 结论。结果包含综合档位 `foundation / developing / independent / advanced`，阅读理解、词汇、语法、书面表达四个维度，以及 `low / medium / high` 置信度、证据数量和可审计原因码。

### 评测 Agent 的客观证据升级

水平评测 Agent 不再用“任务完成多、未使用提示、修订多、标注多”直接推高能力档位。以上信号仍保留为学习过程证据，但不能证明答案正确，更不能替代英语知识测量。Agent 现在沿用既有任务、作答、内容包和语法证据链，汇总：

- 阅读任务首次独立选择是否命中该版本题目的标准答案；
- 题目的 `foundation / standard / advanced` 难度档与阅读题型；
- 语法证据中的独立答对、支持后答对、失败和延迟迁移；
- 主观难度仅作为有界的负荷校正，不替代客观表现；
- 材料“有帮助 / 没帮助”仍只进入材料质量原因码，不改变能力或可评分证据数。

同样正确率会按题目难度形成不同的当前适配估计，但现阶段只是透明、可回归的难度锚定启发式，不是已经标定的 IRT。高置信度要求至少 12 条可评分证据、覆盖至少两个阅读难度档且覆盖不止一个被测维度；单纯增加完成次数不能提高置信度。

词汇维度只有在出现 `vocabulary_in_context` 客观题时才视为直接测量；否则只按阅读表现给出保守负荷并标记 `vocabulary:not_directly_measured`。书面表达目前没有经过教研量表校准的自动评分证据，因此完成或修订表达任务不会提高写作档位，并标记 `writing:not_rubric_scored`。学习画像会把这两类缺口直接展示给用户，而不是把保守占位档伪装成已经测得的能力。

这次升级仍使用原有“反馈入队 → Worker 汇总 → `LevelAssessmentAgent` 决策 → 画像与下一篇材料消费”链路。Agent 的价值体现在跨任务取证、证据分级、受约束判断、可审计解释和后续教学编排，而不是让语言模型凭印象给学习者打分。

学习画像页展示综合档位、已测维度和未测边界。下一次个性化阅读生成会读取最新已完成评测，同时调节词汇负荷、句法负荷、篇章关系和支架强度；低置信度时只允许增加一个主要挑战维度。标准题库的题目档位和后续材料匹配也优先使用当前适配水平，并在没有新评测时兼容原画像快照。用户跳过主观评价时仍可根据客观作答与语法验证更新，但不会伪造难度评价信号。

该实现复用原有难度反馈、Worker、画像和个性化材料生成链路，不存在第二套阅读器、反馈状态机或材料工作区。

## 2. 运行类型与连续创建

`VerticalSliceRun` 新增两个显式字段：

- `run_kind=first_experience | practice`；
- `predecessor_run_id`。

首次运行必须从 `calibration_a` 和校准阅读创建；`practice` 必须引用一个已完成前序运行，从 `matched_reading` 和一条已登记的匹配决策创建。领域层会拒绝混合状态，完成门只对首次运行要求两段校准。

学习 API 新增：

`POST /learner/v1/runs/{workflow_run_id}/continue`

该接口要求前序运行已完成且版本一致。数据库对 `predecessor_run_id` 建立唯一约束；相同前序即使使用新的幂等键再次请求，也只返回已有后续运行，不会并行产生两条体验链。

## 3. 新材料匹配

连续创建会复制前序学习者快照中的显式背景，更新证据数量并生成新的不可变快照。匹配器读取当前学习账号的完整正式阅读历史，优先从未使用的合格材料；全部候选都使用过后，选择最久未使用的材料，再由既有水平与时长策略完成同等新颖度候选之间的排序。

匹配决策使用 `continuous_practice_match_v1`，并按结果保存 `unseen_material_preferred` 或 `least_recent_material_reused` 原因码。首页与完成页的“下一次训练”会优先进入训练队列中已通过语义审核的新材料，没有就绪材料时才回退到上述正式内容池。它仍是确定性保守规则，不是能力估计或远程模型决策；当前开发内容包只有两组正式阅读与配对表达，因此回退池只能验证最久未用轮换，不能证明长期内容供给已经完成。

## 4. 首次校准摘要

首次运行完成两段校准、进入匹配阅读时，学习端先呈现一次校准摘要：

- 本次较稳的部分；
- 接下来优先观察的动作；
- 为什么采用当前匹配负荷；
- 证据仍少、不是最终等级的边界说明。

摘要不展示内部参数、预计分数或精确能力等级。用户确认后进入已分配的匹配阅读；确认状态只保存在当前电脑，存储不可用时安全地再次显示摘要。

## 5. 学习首页与完成页

完成页现在展示本次任务数、帮助使用情况、难度反馈对应的下一步建议，并允许直接开始新材料训练或返回学习首页。

学习首页提供：

- 已保存的英一/英二方向、单次时长和反馈方式；
- 近 7 天账号完成分钟与用户计划的对照；
- 完整闭环次数和全程 H0 次数；
- 服务端分页的运行类型、难度反馈、任务数和帮助使用情况；
- 更新训练设置并重新校准的入口。

最近训练不再由 `localStorage.sessions` 维护。`GET /learner/v1/training-history?page=1&page_size=5` 按当前账号直接查询持久化的 `workflow_runs`、`run_task_completion_events` 和 `run_task_refs`，返回稳定倒序分页、总页数以及完整闭环、全程 H0、任务、帮助使用和近 7 天汇总。首页每页只请求当前 5 条，完成新训练后回到第一页刷新；换浏览器或换电脑登录同一账号仍可读取。

本地存储继续承担未提交草稿、恢复指针、校准摘要确认和旧版本兼容，不再是最近训练、画像训练统计或用户偏好的权威数据源。时长、次数和 H0 数量仍不解释为能力提升。

### 5.1 账号偏好与实际消费链路

`GET/PUT /learner/v1/preferences` 使用 `learner_preferences` 保存当前账号的一份版本化偏好。首次升级时，如果服务端还没有记录，前端会把旧的 `binnagent:learner-preferences:v1:*` 浏览器记录迁移一次，成功后清除旧键；此后换浏览器或电脑登录同一账号可恢复相同设置。左侧导航折叠状态也进入同一记录，不再单独写浏览器缓存。

偏好页不存在只保存不生效的字段：

| 设置 | 实际消费位置 |
| --- | --- |
| 行内辅助出现方式 | 控制选区帮助是主动聚焦、等待点击还是安静隐藏 |
| 反馈详细程度 | 控制纠偏卡片只显示首个行动句、完整反馈或完整反馈加 V1/V2 操作说明 |
| 纠偏语气 | 在不改变反馈标准的前提下切换“先看这一处”与“需要修改” |
| 展示判断依据 | 控制反馈依据摘要是否出现 |
| 允许临时任务 | 控制临时迁移任务入口、标签和自动加入行为 |
| 页面留白 | 控制阅读区密度、字号、行距和最大宽度 |
| 减少动态效果 | 控制滚动方式、过渡与主题动效 |
| 界面皮肤 | 通过主题运行时统一应用色板、层次、装饰与相应布局能力 |

下拉选择统一使用参考 BinnAgent 的自定义 Select：按钮触发器、独立箭头底座、浮层选项、明确选中态、点击外部关闭和键盘方向键/Enter/Escape 操作。

## 6. 自动化证据

新增或扩展验证包括：

- `practice` 从匹配阅读开始且完成门不再要求校准；
- 连续接口拒绝未完成或版本不一致的前序运行；
- 首次匹配到材料 01 后，下一次自动选择材料 02；
- 同一前序运行不能生成第二个后续运行；
- 账号训练历史支持服务端分页与汇总；
- 账号偏好支持读取、首次写入、更新版本和重新读取；
- 反馈详细程度与纠偏语气有独立行为测试；
- 返回用户从学习首页进入 3 步 practice 工作区；
- 新增页面通过 Chromium 主路径、Console/网络错误门禁和 axe 扫描；
- Python/TypeScript 类型检查、单元、契约、集成、Lint 和生产构建通过。

## 7. 明确未完成

- 超过两组正式材料后的长期内容调度与重复间隔；
- 难度反馈驱动的多维升降级策略；
- 延迟任务、无提示迁移任务和阶段切换；
- 用户主动纠正画像、熟题替换和内容问题上报；
- 返回用户的删除数据和隐私控制；
- 真人考生留存、可用性、教师审查与学习效果验证。
- 阅读题尚未经过真实考生样本的项目参数标定、DIF 检查和等值；当前难度锚定不能解释为 IRT 能力量尺。
- 书面表达尚缺双人教研金标、评分量表、评分者一致性和模型误差校准；在此之前维度保持“未直接测量”。

因此本增量把项目从“一次性 60 秒入口和单轮技术切片”推进到了可重复运行的完整桌面体验，但仍属于范围受限的工程验证版本，不是生产学习系统。
